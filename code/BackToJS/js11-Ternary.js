const number = 5;

// let description;
// if (number > 5) {
//     description = "number is larger than 5";
// } else {
//     description = "number is less or equal to 5";
// }

// console.log(description);

// _CONDITION_ ? _VALUE IF TRUE_ : _VALUE IF FALSE_
const description = number > 5 ? "number is larger than 5" : "number is less or equal to 5";
console.log(description);